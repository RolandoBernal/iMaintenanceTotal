@using Microsoft.AspNet.Identity
<div class="navbar navbar-default navbar-fixed-top">

    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            @Html.ActionLink("iMaintenanceTotal", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
        </div>
        <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                
                @if (Request.IsAuthenticated)
                {
                    <li>@Html.ActionLink("My Reminders", "Index", "MaintTask")</li>
                    <!--<li>@Html.ActionLink("Add New Reminder", "Index", "MaintTask")</li>-->
                    <li><button class="btn btn-primary-add-new-rem" data-toggle="modal" data-target="#maint-task-modal">Add New Reminder</button></li>
                }
                else
                {
                    <li>@Html.ActionLink("Home", "Index", "Home")</li>
                    <li>@Html.ActionLink("About", "About", "Home")</li>
                    <li>@Html.ActionLink("Contact", "Contact", "Home")</li>
                }
                
            </ul>
            @Html.Partial("_LoginPartial")
        </div>
    </div>
</div>
